// Type of a user
export type TUser = {
    code: string;
    name: string;
    imageURL: string;
    spent: number;
    owed: number;
    difference: number;
    createdAt: string;
    updatedAt: string;
}
